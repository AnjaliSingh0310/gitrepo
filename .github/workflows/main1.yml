name: Basic CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:

  # --------------------------------------------------
  # 1️⃣ Build & Test Job
  # --------------------------------------------------
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # -----------------------------
      # Setup Java (for Maven)
      # -----------------------------
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: maven   # ✅ Maven cache enabled

      # -----------------------------
      # Setup .NET
      # -----------------------------
      - name: Set up Dotnet
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      # -----------------------------
      # Cache NuGet packages
      # -----------------------------
      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      # -----------------------------
      # Build Maven project
      # -----------------------------
      - name: Build with Maven
        working-directory: simple-maven-project
        run: mvn clean verify


  # --------------------------------------------------
  # 2️⃣ Code Quality Job (Separate)
  # --------------------------------------------------
  code-quality:
    runs-on: ubuntu-latest
    needs: build   # ✅ Runs after build succeeds

    steps:
      - uses: actions/checkout@v4

      # -----------------------------
      # Setup .NET
      # -----------------------------
      - name: Set up Dotnet
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      # -----------------------------
      # Cache NuGet packages
      # -----------------------------
      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      # -----------------------------
      # Cache SonarScanner
      # -----------------------------
      - name: Cache SonarScanner
        uses: actions/cache@v4
        with:
          path: ~/.dotnet/tools
          key: ${{ runner.os }}-sonarscanner

      # -----------------------------
      # Run Code Quality
      # -----------------------------
      - name: Run Code Quality Parametrised
        run: |
          chmod +x multi-project-scanner1.sh
          ./multi-project-scanner1.sh \
            --project-key-prefix as-22 \
            --org AS_22 \
            --host-url https://sonarcloud.io \
            --token "33f49840787e6388b6ceab0e11da146d09902636"
