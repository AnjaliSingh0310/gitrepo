trigger:
- main

pool:
  name: Default   # Self-hosted agent pool

jobs:
- job: Build
  displayName: Run Extension Build
  steps:
    - task: CavissonScanProjects@1
      inputs:
        cavissonScannerServiceConnection: 'sonar22'
        project: 'cav_proj_001'
