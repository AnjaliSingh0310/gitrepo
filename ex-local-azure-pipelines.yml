trigger:
- main

pool:
  name: Default   # Self-hosted agent pool

jobs:

- job: EnvironmentCheck
  displayName: Check Build Environment
  steps:
    - checkout: self

    - task: NodeTool@0
      inputs:
        versionSpec: '18.x' # Replace with your required version
      displayName: Install Node.js

    - script: node --version
      displayName: Check Node.js Version

    - script: npm --version
      displayName: Check npm Version

    - script: ls -la
      displayName: List Files

    - script: java -version
      displayName: Check Java

    - script: dotnet --version
      displayName: Check Dotnet

    - script: mvn -version
      displayName: Check Maven

- job: Build
  displayName: Run Extension Build
  steps:
    - task: run-quality@1
      inputs:
        projectKey: my_unique_testing_key_12
        hostUrl: 'https://sonar.cav-test.com:4444/sonar'
        solution: 'simple-maven-project'
        sonarToken: sqp_631dbc4743b159c42953bd814ecab85f6d86283b